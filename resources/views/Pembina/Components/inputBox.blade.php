<div class="w-full">
    <div id="{{ $textareaContainer }}" class="w-4/5 h-96 border border-customBlue p-4 rounded-lg">
        <h3 class="text-lg font-semibold mb-2">{{ $title }}</h3>
        <textarea id="{{ $kegiatanTextArea }}" class="w-full mt-2 h-36 resize-none border border-customBlue"
            placeholder="Masukkan {{ $title }}"></textarea>
        <textarea id="{{ $kegiatanTextArea }}Komentar" class="w-full mt-2 h-36 resize-none border border-customBlue"
            placeholder="Masukkan komentar"></textarea>
    </div>
</div>
